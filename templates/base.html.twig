<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}My Book List{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            padding-top: 50px;
        }

        .container {
            max-width: 1200px;
        }

        .navbar {
            background-color: #343a40;
        }

        .navbar-brand {
            color: white;
        }

        .navbar-nav .nav-link {
            color: white;
        }

        .table th, .table td {
            vertical-align: middle;
        }

        .flash-message {
            margin-top: 20px;
        }

        .flash-message.success {
            color: green;
        }

        .flash-message.error {
            color: red;
        }

        .card {
            margin-top: 20px;
        }

        .btn-custom {
            background-color: #007bff;
            color: white;
            border: none;
        }

        .btn-custom:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<div class="container">
    <nav class="navbar navbar-expand-lg">
        <a class="navbar-brand" href="{{ path('book_index') }}">Book List</a>
        <div class="navbar-nav">
            <a class="nav-link" href="{{ path('book_index') }}">Home</a>
            <a class="nav-link" href="{{ path('book_add') }}">Add Book</a>
        </div>
    </nav>

    {% block body %}
    {% endblock %}
</div>
</body>
</html>
